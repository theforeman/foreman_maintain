# Directory where the logs are stored.
# The default file is named as /log/foreman_maintain.log
:log_dir: 'log'

# Logger levels: mention one of debug, info, warning, error, fatal
:log_level: 'unknown'

# Mention log file size in KB. Default set to 10000KB.
# :log_file_size: 10000

# Mention definitions directories. Default
:definitions_dirs:
  - 'test/lib/support/definitions'
  - 'test/lib/support/additional_definitions'

# Mention file path to store data
:storage_file: 'test/lib/config/data_test.yml'

# Mention path where foreman-proxy settings stored on filesystem
# :foreman_proxy_settings_path: '/etc/foreman-proxy/settings.yml'
